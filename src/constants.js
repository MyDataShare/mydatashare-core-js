/**
 * Authorization parameters given in URL fragment portion.
 * @type {string}
 */
export const RESPONSE_MODE_FRAGMENT = 'fragment';

/**
 * Authorization parameters given in URL search parameters.
 * @type {string}
 */
export const RESPONSE_MODE_QUERY = 'query';
